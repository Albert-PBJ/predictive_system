apify_client==2.5.0
python-dotenv==1.1.0
apify_shared==2.2.0
asgiref==3.11.1
colorama==0.4.6
Django==6.0.4
django-cors-headers==4.9.0
django-stubs==6.0.3
django-stubs-ext==6.0.3
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
PyJWT==2.13.0
djangorestframework-stubs==3.16.9
impit==0.12.0
more-itertools==11.0.2
numpy==2.4.4
# Lectura de los archivos .xlsx de la empresa (resources/) en el comando seed_company_data.
openpyxl==3.1.5
# Tasa de cambio: fuente primaria de fetch_exchange_rate (BCV oficial estable +
# paralela best-effort). Si no está instalada, el comando cae a la API HTTP pyDolarVe.
# Arrastra varias dependencias (requests, beautifulsoup4, sqlalchemy, curl-cffi).
pyDolarVenezuela==2.0.3
# Opcional: solo se usa si USE_LLM_ENRICHMENT=True (identificación de competidores vía DeepSeek).
# DeepSeek es compatible con la API de OpenAI, por eso reutilizamos este SDK.
openai>=1.40,<2
# Opcional y PESADO (~arrastra torch, cientos de MB): solo se usa si USE_VISION_PRICE_OCR=True.
# OCR por red neuronal (EasyOCR: CRAFT + CRNN) para leer el precio de la imagen de un
# post de Instagram cuando ni el caption ni el LLM lo encontraron. Instálalo aparte si
# vas a activarlo: pip install easyocr
# easyocr>=1.7,<2
pandas==3.0.2
# Capa de Machine Learning (módulo predictivo, apps/analytics): regresión y árboles
# de decisión (scikit-learn), gradient boosting (XGBoost) y serialización de modelos
# (joblib). Ruedan en Python 3.14 (wheels cp314 disponibles).
scikit-learn==1.9.0
xgboost==3.2.0
joblib==1.5.3
psycopg==3.3.3
psycopg-binary==3.3.3
python-dateutil==2.9.0.post0
six==1.17.0
sqlparse==0.5.5
types-PyYAML==6.0.12.20260408
typing_extensions==4.15.0
tzdata==2026.1
